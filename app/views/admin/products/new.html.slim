h1.jumbotron Add a Product

.container
  = simple_form_for [:admin, @category], class: "form-horizontal" do |f|
    = f.input :name
    = f.button :submit

.container
  = simple_form_for [:admin, @product], class: "form-horizontal" do |f|
    = f.input :name
    = f.input :brand
    = f.input :description
    = f.input :price
    = f.input :quantity
    = f.input :size
    = f.input :color

    = f.association :categories, collection: Category.all, as: :check_boxes

    = f.filestack_field :filelocation, 'Upload Photo'

    = f.button :submit

  = link_to 'Back to Product List', admin_products_path
